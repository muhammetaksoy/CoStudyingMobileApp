<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lottie="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             Title="Akış"
             x:Class="YeatyAppMobile.Views.HomeTabbedPages.FeedPage">

    <ContentPage.IconImageSource>

        <FontImageSource FontFamily="{StaticResource FontAwesomeLight}" Glyph="compass"></FontImageSource>

    </ContentPage.IconImageSource>
    <ContentPage.Content>
        <StackLayout x:Name="stackGeneral"  Margin="0" Padding="0" Spacing="0" IsVisible="True">

            <StackLayout x:Name="stackWelcomeText" Margin="12">
                <Label x:Name="lblWelcome" FontFamily="CustomBold" FontSize="Medium">
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Hoşgeldin"  TextColor="{StaticResource defaultRedColor}"></Span>
                            <Span Text=" "></Span>
                            <Span x:Name="spanUserFirstName" Text="Ulaş" TextColor="{StaticResource defaultRedColor}"></Span>
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </StackLayout>

           
        </StackLayout>
    </ContentPage.Content>
</ContentPage>